{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist/public",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/oauth/callback", "destination": "/" },
    { "source": "/workspace", "destination": "/" }
  ]
}
